# Test-only dependencies for the pytest suite (test_main.py / conftest.py).
# Not required to run the proxy itself -- see requirements.txt for that.
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.24
respx>=0.21
