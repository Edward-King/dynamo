fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
PyYAML>=6.0
Pillow>=11.0
natsort>=8.4
python-multipart>=0.0.20

# test dependencies (see tests/README.md). httpx is required by FastAPI's
# TestClient (starlette.testclient) and is not pulled in by fastapi itself.
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
